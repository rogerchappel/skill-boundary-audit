# Examples

Run:

```bash
node ../bin/skill-boundary-audit.js review-this-skill.md --format markdown
```

The example intentionally includes external action language so the report has findings to inspect.
